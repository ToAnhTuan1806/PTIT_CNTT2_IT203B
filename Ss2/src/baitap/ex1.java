package baitap;

public class ex1 {
    // Kiểm tra xem một User có phải là Admin hay không (trả về true/false) -> dùng Predicate để ktra 1 đk trả về giá trị boolean
    // Chuyển đổi một đối tượng User thành String chứa username → dùng Function để nhận vào một giá trị và trả về một giá trị khác sau khi xử lý
    // In thông tin chi tiết của User ra Console → dùng Consumer để nhận dữ liệu vào và thực hiện hành động, không trả về kết quả
    //Khởi tạo một đối tượng User mới với các giá trị mặc định → dùng Supplier để tạo và cung cấp một đối tượng, không cần đầu vào
}
